import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'boxer.management.product.client',
  webDir: 'www'
};

export default config;
